<%= form_for @writer, remote: true do |f| %>
  <% if @writer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@writer.errors.count, "error") %> prohibited this car from being saved:</h2>

      <ul>
        <% @writer.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>
  <br/>
  <div class="actions">
    <%= f.submit class: "submit_button"%>
  </div>
<% end %>